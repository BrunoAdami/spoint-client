import React from 'react';
import CLOSE_ICON from '../../assets/close-icon.png';

const CloseIcon = (props) => <img src={CLOSE_ICON} alt="Close icon" title="Close icon" {...props} />;

export default CloseIcon;
