import React from 'react';
import GO_BACK_ICON from '../../assets/go-back-icon.png';

const Logo = (props) => <img src={GO_BACK_ICON} alt="Spoint Logo" title="Spoint" {...props} />;

export default Logo;
