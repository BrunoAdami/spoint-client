import React from 'react';
import GO_BACK_ICON from '../../assets/go-back-icon.png';

const Logo = (props) => <img src={GO_BACK_ICON} alt="Go back icon" title="Go back icon" {...props} />;

export default Logo;
